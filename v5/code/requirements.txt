# METIS v4 — Python dependencies
# Tested with Python 3.9+

# LLM
google-genai>=1.0.0

# HTTP / WS
httpx>=0.27.0
websockets>=12.0

# Schema / config
pydantic>=2.5.0
python-dotenv>=1.0.0
pyyaml>=6.0

# Numeric
numpy>=1.24.0

# Dashboard (separate process)
streamlit>=1.30.0

# Resource monitoring (optional but recommended)
psutil>=5.9.0

# Test (dev only)
pytest>=8.0.0
